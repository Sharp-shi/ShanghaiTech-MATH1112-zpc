# <center>hw_10(2)<center>

## 习题5.1

### 4. 证明下面的映射 $T: \mathbb{R}^2 \to \mathbb{R}^2$ 是 $\mathbb{R}^2$ 的线性变换并求出其矩阵
#### (3) 先做关于 $x$ 轴的反射, 再做以直线 $y = x$ 为轴的反射.

**解：**

$R_x = \begin{pmatrix} 1 & 0 \\ 0 & -1 \end{pmatrix}$

$R_{y=x} = \begin{pmatrix} 0 & 1 \\ 1 & 0 \end{pmatrix}$

$T = R_{y=x} R_x = \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$

故为线性变换，矩阵 $\begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$



#### (4) 先做 $y$ 轴的反射, 再绕原点旋转角度 $\frac{\pi}{4}$.

**解：**

$$ R_y =  \begin{pmatrix} -1 & 0 \\ 0 & 1 \end{pmatrix} $$ 

$$ R_{\frac{\pi}{4}} =  \begin{pmatrix} \frac{\sqrt{2}}{2} & -\frac{\sqrt{2}}{2} \\ \frac{\sqrt{2}}{2} & \frac{\sqrt{2}}{2} \end{pmatrix} $$ $$ T = R_{\frac{\pi}{4}} \cdot R_y  =  \begin{pmatrix} -\frac{\sqrt{2}}{2} & -\frac{\sqrt{2}}{2} \\ -\frac{\sqrt{2}}{2} & \frac{\sqrt{2}}{2} \end{pmatrix} $$ 

故 $ T $为线性变换，矩阵  $$ \begin{pmatrix} -\frac{\sqrt{2}}{2} & -\frac{\sqrt{2}}{2} \\ -\frac{\sqrt{2}}{2} & \frac{\sqrt{2}}{2} \end{pmatrix} $$




## 习题5.2
### 1. 下面关于 $T(x, y)$ 的公式定义了映射 $T : \mathbb{R}^2 \to \mathbb{R}^2$. 判断各小题的映射是否为：(i) 单射，(ii) 满射，(iii) 可逆映射. 

#### (6) $T(x, y) = [2^x, 2^y]$，

**解：**
(i) 是
(ii) 不是
(iii) 不是



#### (10) $T(x, y) = [3x + 2y, 5x - 6y]$. 

**解：**
(i) 是
(ii) 是
(iii) 是



### 2. 下面关于 $T(x, y, z)$ 的公式定义了线性映射 $T: \mathbb{R}^3 \to \mathbb{R}^3$. (i) 求出这些线性映射的核, (ii) 求出这些线性映射的像, (iii) 判断这些线性映射是否可逆.

#### (5) $T[x, y, z] = [x + y, y + z, z + x]$,

**解：**

(i) $\{0\}$

(ii) $\mathbb{R}^3$

(iii) 可逆



#### (6) $T(x, y, z) = [x - y, y - z, z - x]$.

**解：**
(i) $\left\{c\begin{pmatrix}1\\ 1\\ 1\end{pmatrix}, c\in \mathbb{R}\right\}$

(ii) $\{(u_1, u_2, u_3)\in \mathbb{R}^3 \mid u_1+u_2+u_3=0\}$

(iii) 不可逆





### 4. 设 $A : \mathbb{R}^2 \to \mathbb{R}^2$ 是线性算子(也称为线性变换)，定义为 $A(x, y) = (-x, y)$. 

#### (1) 求 $A$ 在标准基下的矩阵 $A$；

**解：**
$$
\left( \begin{array}{cc} -1 & 0 \\ 0 & 1 \end{array} \right)
$$



#### (2) 求 $A$ 在基 $u_1 = (1, 1), \, u_2 = (-1, 1)$ 下的矩阵 $B$；

**解：**
$$
B = \left( \begin{array}{cc} 0 & 1 \\ 1 & 0 \end{array} \right)
$$




#### (3) 求从基 $u_1, \, u_2$ 到基 $v_1 = (2, 1), \, v_2 = (1, 0)$ 的转换矩阵 $S$，再通过计算 $S^{-1}BS$ 求出 $A$ 在基 $v_1, \, v_2$ 下的矩阵. 

**解：**
$$
S = U^{-1} V = \left( \begin{array}{cc} \frac{3}{2} & 1 \\ -1 & -1 \end{array} \right)
$$

$$
A_v = S^{-1} BS = \left( \begin{array}{cc} 1 & 0 \\ -4 & -1 \end{array} \right)
$$






#### (4) 求从标准基到基 $v_1, \, v_2$ 的转换矩阵 $T$，再通过计算 $T^{-1}AT$ 求出 $A$ 在基 $v_1, \, v_2$ 下的矩阵. 比较(3)和(4)的计算的繁简并回顾例4.8的解题方法. 

**解：**
(4) 
$$
T = \left( \begin{array}{c} 2&1 \\ 1&0 \end{array} \right)
$$

$$
A_v = T^{-1} AT = \left( \begin{array}{cc} 1 & 0 \\ -4 & -1 \end{array} \right)
$$




### 4. 定义线性算子 $A : \mathbb{R}^3 \to \mathbb{R}^3$ 如下

$$
A \begin{pmatrix}
x_1 \\
x_2 \\
x_3
\end{pmatrix} =
\begin{pmatrix}
2x_1 - x_2 - x_3 \\
2x_2 - x_1 - x_3 \\
2x_3 - x_1 - x_2
\end{pmatrix}
$$

#### (1) 求 $A$ 在标准基下的矩阵. 

**解：**
$$
A = \begin{pmatrix} 2 & -1 & -1 \\ -1 & 2 & -1 \\ -1 & -1 & 2 \end{pmatrix}
$$



#### (2) 确定从标准基到基 $u_1 = [1, 1, 0]$, $u_2 = [1, 0, 1]$, $u_3 = [0, 1, 1]$ 的转换矩阵 $T$. 

**解：**
$$
T = \begin{pmatrix} 1 & 1 & 0 \\ 1 & 0 & 1 \\ 0 & 1 & 1 \end{pmatrix}
$$




#### (3) 求 $A$ 在基 $u_1$, $u_2$, $u_3$ 下的矩阵. 

**解：**
$$
A_u = T^{-1} A T = \begin{pmatrix} 2 & -1 & -1 \\ -1 & 2 & -1 \\ -1 & -1 & 2 \end{pmatrix}
$$




### 5. 命 $A = \begin{pmatrix} 3 & -1 & -2 \\ 2 & 0 & -2 \\ 2 & -1 & -1 \end{pmatrix}$. 定义线性算子 $A : \mathbb{R}^3 \to \mathbb{R}^3$. 求 $A$ 在基 $v_1 = [1, 1, 1]$, $v_2 = [1, 2, 0]$, $v_3 = [0, -2, 1]$ 下的矩阵. 

**解：**

$$
T = \left( \begin{array}{ccc} 1 & 1 & 0 \\ 1 & 2 & -2 \\ 1 & 0 & 1 \end{array} \right)
$$

$$
A_v = T^{-1}AT = \left( \begin{array}{ccc} 0 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{array} \right)
$$



### 8. 证明: 如果方阵 $A$ 与 $B$ 相似, $E$ 是同阶的单位矩阵, 那么 $E + A$ 与 $E + B$ 相似.

**解：**

$$
E + B = E + T^{-1} A T
$$

$$
= T^{-1} E T + T^{-1} A T
$$

$$
= T^{-1} (E + A) T
$$

故相似



### 10. (1) 证明如下两个 $n$ 阶方阵是相似的：

$$
\left(
\begin{array}{ccccc}
\lambda & 1 & 0 & \cdots & 0 & 0 \\
0 & \lambda & 1 & \cdots & 0 & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
0 & 0 & 0 & \cdots & \lambda & 1 \\
0 & 0 & 0 & \cdots & 0 & \lambda
\end{array}
\right),
\left(
\begin{array}{ccccc}
\lambda & 0 & \cdots & 0 & 0 & 0 \\
1 & \lambda & \cdots & 0 & 0 & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
0 & 0 & \cdots & 1 & \lambda & 0 \\
0 & 0 & \cdots & 0 & 1 & \lambda
\end{array}
\right)
$$

**解：**
取 $T = 
\begin{pmatrix}
0 & 0 & \cdots & 0 & 1 \\
0 & 0 & \cdots & 1 & 0 \\
\vdots & \vdots & \ddots & \vdots & \vdots \\
0 & 1 & \cdots & 0 & 0 \\
1 & 0 & \cdots & 0 & 0
\end{pmatrix}$

$T^{-1} AT = B \quad $故相似



### 12. 设方阵 $A_1, \, A_2, \, \ldots, \, A_k$ 分别与方阵 $B_1, \, B_2, \, \ldots, \, B_k$ 相似，

$$
A = 
\begin{pmatrix}
A_1 & & & 0 \\
& A_2 & & \\
& & \ddots & \\
& & & A_k
\end{pmatrix}, \quad B = 
\begin{pmatrix}
B_1 & & & 0 \\
& B_2 & & \\
& & \ddots & \\
& & & B_k
\end{pmatrix}
$$

### 证明：$A$ 与 $B$ 相似. 

**解：**
$$
B_i = P_i^{-1} A_i P_i
$$

$$
P = 
\begin{pmatrix}
P_1 &\dots& 0 \\
\vdots &\ddots& \vdots \\
0 &\dots& P_k
\end{pmatrix}
$$

$$
P^{-1}AP =
\begin{pmatrix}
P_1^{-1}A_1P_1 &\dots& 0 \\
\vdots &\ddots& \vdots \\
0 &\dots& P_k^{-1}A_kP_k
\end{pmatrix}
=
\begin{pmatrix}
B_1 &\dots& 0 \\
\vdots &\ddots& \vdots \\
0 &\dots& B_k
\end{pmatrix}
= B
$$

故相似
